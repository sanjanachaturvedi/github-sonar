--- 
artifacts: 
  discard-paths: true
  files: 
    - target/PrintInformation-1.0-SNAPSHOT.jar
phases: 
  build: 
    commands: 
      - "echo Entered the build phase..."
      - "echo Build started on `date`"
      - "mvn install"
version: 0.2
